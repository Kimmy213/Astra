// Copy this file to Secrets.swift and paste in your own key from https://api.nasa.gov
// Secrets.swift is gitignored so the real key never reaches the repository.

enum Secrets {
    static let nasaAPIKey = "YOUR_KEY_HERE"
}
